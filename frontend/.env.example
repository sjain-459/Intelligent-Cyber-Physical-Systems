# Base URL of the CTMAS backend (api.py). Baked in at build time by Vite.
# Leave unset for local development (defaults to http://localhost:8001).
# In production, set this to the deployed backend's URL, e.g.:
# VITE_API_BASE_URL=https://ctmas-api.onrender.com
VITE_API_BASE_URL=
